/*opens the links in the page in other tabs (do NOT use the in current tab option, it is BROKEN!)*/

var a = '';for (var ln = 0; ln < document.links.length; ln++) {  var lk = document.links[ln];  if (lk != "javascript:void(0)") {    a += " " + lk;  }}var lna = a.split(" ");var ln = [...new Set(lna)];var numln = [];for (i=0; i<ln.length-1; i++) {  numln[i] = "\n"+i+": "+ln[i];  if (numln[i].length-1 > 70) {    numln[i] = numln[i].substring(0,70)+"...";  }}var length = ln.length - 1;var num = prompt("Open tabs? \n" + numln, length);if (confirm("open in new tabs?")) {  if (num != isNaN) {    let st = prompt("start at:", 0);    let z = Number(num)+Number(st);    var num = z;    alert(num);    for (i = st; i < num; i++) {      window.open(ln[i]);    }  }} else if (confirm("open in current tab?")) {  var tabs = Math.ceil(Math.sqrt(num));  for (i = 0; i < 100 / tabs; i++) {    for (a = 0; a < 100 / tabs; a++) {      var frame = document.createElement("iframe");      var source = ln[i * a];      frame.src = source;      frame.style.position = "fixed";      frame.style.bottom = (i * 100 / tabs) + "%";      frame.style.right = (a * 100 / tabs) + "%";      frame.style.height = screen.availHeight / tabs + "%";      frame.style.width = screen.availWidth / tabs + "%";      frame.style.zIndex = "100000";      document.body.appendChild(frame);    }  }}
